version: "3"
services:
  node:
    build: .
    command: "npm run build"
    working_dir: /usr/src/app
    ports:
      - 3000:3000
  nginx:
    build: /usr/src/app/nginx
    ports:
      - 80:80
    depends_on:
      - node